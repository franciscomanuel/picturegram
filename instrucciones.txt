Instalar virtualenv:
	pip3 install virtualenv
Crear el entorno virtual:
	virtualenv env
Iniciar el entorno virtual
	env\Scripts\activate.bat
Instalar Django:
	pip3 install django -U
Comprobar que todos los modulos esten instalados
	pip3 freeze
Para desactivar el entorno usamos
	env\Scripts\deactivate.bat
Crear el proyecto:
	Una vez iniciado el entorno virtual, el proyecto se puede crear en el directorio que uno desee, fuera de este entorno virtual.
	django-admin startproject picturegram .
Para ver los comandos del proyecto ejecutar:
	python manager.py
Para lanzar el servidor:
	python manager.py runserver


Exíste un módulo de python para debugear desde consola:
	import pdb; pdb.set_trace()

	Recargar la página y estará disponible.
	En caso de terminar de debuggear y querer continuar con la ejecución escribir en la consola "c" + "enter"

Para crear una app dentro de nuestra aplicación usamos manager.py
	python manage.py startapp posts


Configurar una BD de tipo sqlite:
	python manager.py migrate

Después de aplicar cambios en modelos:
	python manager.py makemigrations
	python manager.py migrate

Usar este gestor de BD online: https://sqliteonline.com/

Para abrir un shell de python:
	python manage.py shell

Nos permitirá importar modelos de nuestra app.

Ejemplo:

	python manage.py shell

	>> from posts.models import User
	>> francisco = User.objects.create(email='fmsanchezgr@gmail.com',
	password='12345678',
	first_name='Francisco',
	last_name='Sanchez')

	o

	francisco = User()
	francisco.email='fmsanchezgr@gmail.com'
	francisco.password='12345678'
	francisco.first_name='Francisco'
	francisco.last_name='Sanchez'
	francisco.save()

	modificar un valor:
	>> francisco.email = 'fmsanchezgr2@gmail.com'
	>> francisco.save()

	Borrar un registro
	>> francisco.delete()


	Mirar el user_script file


	Para recuperar un usuario:
	>> user = User.objects.get(pk='1')

	Recuperar usuarios aplicando filtros
	>> users_filter = User.objects.filter(email__endswith='.com')

	Recuperar todos los users:
	>> users = User.objects.all()

	Actualizar los usuarios filtrados y hacer que sean todos admin
	>>  User.objects.filter(email__endswith='.com').update(is_admin=True)

	Ver el resultado
	>> users_filter = User.objects.filter(email__endswith='.com')
	for u in users_filter:
		print(u.email, ':', u.is_admin)


Pero Django dispone de un sistema de administración de usuarios propio.
	1) Accedemos a la shell: 
		python manage.py shell
	2) Creamos un usuario:
		from django.contrib.auth.models import User
		u = User.objects.create_user(username='francisco', password='admin123')

	La password se cifra con un cifrado sha-256

Creamos un superusuario:
	python manage.py createsuperuser

Ahora podemos acceder al area de administración con el superuser creado:
	localhost:8000/admin


Después de todo esto se ha borrado la BD y el modelo creado.


Creamos un nuevo modelo User que tenga una oneToOne con el user de Django:
	website
	biography
	phone_number
	profile_picture
	created
	modified

	python manage.py startapp users

Después de crear el modelo, como profile_picture es de tipo ImageField, necesitamos installar Pillow para que funcione. Dentro de nuestro entorno, instalar: 
	pip install pillow

Después tenemos que registrar nuestro modelo en admin.py para que se vea reflejado en el área de administración.



Validación de formularios